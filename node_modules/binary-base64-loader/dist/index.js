"use strict";

var _require = require('loader-utils'),
    getOptions = _require.getOptions;

var validateOptions = require('schema-utils');

var schema = require('./options.json');

module.exports = function binaryBase64Loader(source) {
  var options = getOptions(this);
  validateOptions(schema, options, {
    name: 'Binary Base64 Loader',
    baseDataPath: 'options'
  });
  var esModule = typeof options.esModule !== 'undefined' ? options.esModule : true;
  return "".concat(esModule ? 'export default' : 'module.exports =', " ").concat(JSON.stringify(source.toString('base64')), ";");
};

module.exports.raw = true;